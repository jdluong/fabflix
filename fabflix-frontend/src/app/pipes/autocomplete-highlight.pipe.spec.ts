import { AutocompleteHighlightPipe } from './autocomplete-highlight.pipe';

describe('AutocompleteHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new AutocompleteHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
